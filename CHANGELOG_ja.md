# 変更履歴

このプロジェクトへの注目すべき変更点はこのファイルに記録されます。

この形式は [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) に基づいており、
このプロジェクトは [セマンティック バージョニング](https://semver.org/spec/v2.0.0.html) に準拠しています。

## [0.3.2] - 2026-03-17

### 修正

- Markdown エクスポートダイアログにおいて、`.md` ファイルを明示的に選択できるように修正。

## [0.3.1] - 2026-03-16

### 追加

- クリックやダブルクリック時の意図しないドラッグ動作を防止するための、距離ベースのドラッグしきい値を導入。

### 修正

- クリップボード貼り付け時、画像のメタデータが混入したり、外部テキストが内部データを上書きしたりする不具合を修正。

## [0.3.0] - 2026-03-10

### 追加

- 取り消し線（strikethrough）テキストスタイルのサポートとヘルプモーダルの更新。
- タッチデバイスサポートのための、ドラッグ＆ドロップ処理のポインターイベントへの移行。

### リファクタリング・アーキテクチャ

- 巨大なオブジェクト（`MindMapController`、`MindMapService`、`SvgRenderer`）の分割・解体。
- 専用サービス（`ViewportService`、`NavigationService`、`FileIOService`、`ThemeService`）の抽出。
- インタラクション処理を管理するための `InteractionOrchestrator` および `CommandBus` の実装。
- `Node` エンティティからのプレゼンテーション状態の分離。

### 修正

- インタラクション処理のリファクタリングに伴う画像貼り付け機能のリグレッション修正。
- レイアウトのバグ修正および、画面外のノードに起因するE2Eテストのタイムアウトの解消。
- テストファイルにおける型定義とLintエラーの解消。

### ドキュメント

- Nodeのプレゼンテーション分離やインタラクション処理のリファクタリングを反映したアーキテクチャ設計書の更新。
- 取り消し線機能に関する日本語版 README の更新。

## [0.2.3] - 2026-02-22

### 追加

- 差分レンダリングの実装と選択スタイルの永続化バグの修正。
- カスタムコマンド登録APIの実装とコマンドパレットの統合。
- コンストラクタのオプションによるコマンドパレット機能の無効化のサポート。
- アイコンの説明とキーボードショートカットを備えたヘルプモーダルの実装。
- コマンドパレットに青色と赤色の数字アイコン（0〜9）を追加。
- `Shift+Click` による複数選択ロジックの実装。

### 修正

- devcontainerの `postCreateCommand.sh` を bash で実行するように修正。
- `TypedEventEmitter` をエクスポートし、d.tsでイベントハンドリングメソッドを公開。
- 親子階層をコピーする際のノード重複の防止。
- 切り取り（カット）操作および「貼り付けの元に戻す（Undo）」操作後のフォーカスの復元。
- 型チェックおよびLintエラーの解消。

### パフォーマンス

- `findNode` 検索を O(1) にするためのノードIDインデックス（HashMap）の実装。
- `measureNode` および `getNodeHeight` のキャッシュ実装。

### リファクタリング

- `layout_regression` E2Eテストを5つの独立したフェーズテストに分割し、固定タイムアウトを削減して最適化。
- ヘルプモーダルのロジックを専用コンポーネントに抽出し、動的i18nサポートを追加。
- 選択状態の復元と祖先探索ロジックの統合。
- ロジックのテストを `src/features` に、UIのテストを `tests/components` に分離（Package by Feature への移行）。
- トランザクション処理・テーマスタイリング・共有カーネルなどの全体的なアーキテクチャの大規模リファクタリングの実施。

## [0.2.2] - 2026-02-14

### 追加

- VS Code 連携をサポートするためのイベントシステムの強化。
- ファイル I/O インターフェースの抽象化、および XMind/SVG 処理ロジックの抽出。

### 修正

- レイアウトのリグレッション確認におけるタイムアウト値を調整し、E2E テストを安定化。
- `KakidashCommandArgs` を定義し、`KakidashEvents.ts` における型エラーを解消。

### その他

- 開発環境のセットアップおよび Turbo タスク設定の最適化。

## [0.2.1] - 2026-02-12

### 追加

- Markdown エクスポート機能。
- 画像エクスポート（PNG/SVG）機能。
- XMind インポート機能。

### 変更

- コマンドパレットの項目の並び順を変更。

### 修正

- CIのタイムアウトを引き起こすTurboの並列実行の問題を修正。

## [0.2.0] - 2026-02-07

### 追加

- ノードごとの幅調整機能（キーボードショートカット `Shift + Left/Right` およびAPI経由）。
- ノードエディタの幅がカスタマイズされたノード幅に追従するように変更。

### 変更

- デフォルトのキーボードショートカットを更新:
  - ノード編集: `i`, `Space`, `F2`
  - 太字: `Shift + b`
  - 斜体: `Shift + i`
- アーキテクチャ分離のため `MindMapController` と `SvgRenderer` を疎結合化。
- 依存関係を最新バージョンに更新 (`chore/update-dependencies`)。

### その他

- End-to-End (E2E) テストを `turbo:ci` チェックに統合。
- CI/CDワークフローを更新し、Playwrightブラウザのインストールをサポート。

## [0.1.13] - 2026-02-05

### 修正

- 画像ノードの元の寸法を永続化することで、過度な縦方向の隙間やレイアウトの問題を修正しました。
- 折りたまれたノードの子要素が親の高さ計算に影響を与えないよう、レイアウトロジックを修正しました。

## [0.1.2] - 2026-01-29

### 修正

- ノードのレイアウト崩れ（重なりや不正な折り返し）を解消するため、`SvgRenderer` のサイズ計算ロジックを v0.1.0 の仕様に戻しました。
- コマンドパレットのラベル表記を修正: `🗑️ 削除 (Delete)` → `🗑️ Delete`。

### その他

- レイアウトの退行（リグレッション）を防ぐためのテスト `tests/presentation/LayoutConsistency.test.ts` を追加。

## [0.1.1] - 2026-01-28

### 追加

- ノードへのアイコン設定機能の追加（チェック、スター、警告など）。
- コマンドパレット (`m` キー) によるアイコン選択およびノード検索機能。
- 公開 API `Kakidash.updateNode` へのアイコン更新サポートの追加。
- `presentation/resources/Icons.ts` による一貫したアイコンリソース管理。

### 変更

- コマンドパレットのアイテムスタイルの微調整（ボールド解除）。
- ソフトウェアアーキテクチャ設計書と README のアイコン機能への対応。

### 修正

- `SvgRenderer` におけるアイコン付きノードの幅計算とレイアウトの不整合の修正。
- アイコン画像が読み込まれていない場合のレンダリングクラッシュの修正。

## [0.1.0] - 2026-01-25

### 追加

- 自動化されたバージョンアップワークフローとプロジェクトルールの追加。
- Mermaid ダイアグラムを含むソフトウェアアーキテクチャドキュメントの作成。
- ビルドの最適化と開発者体験向上のための pnpm および Turbo の導入。
- クリーンアーキテクチャ実現のための `IdGenerator` インターフェースと `CryptoIdGenerator` 実装。
- ズームリセット機能とショートカットキー設定の強化。
- ボーダーおよび接続線のカスタムスタイル継承機能。
- スタイル関連 API への詳細な TSDoc コメントの追加。

### 変更

- `domain`, `application`, `presentation`, `infrastructure` の各レイヤーへのディレクトリ構造の再編。
- パッケージ管理ツールを `npm` から `pnpm` へ移行。
- `Kakidash` コンストラクタを拡張し、`maxNodeWidth` や `customStyles` をオプションで受け取れるように変更。

### 修正

- 重複したショートカット処理の削除による、貼り付けが二重に行われる問題の修正。
- ズームリセット時の表示中心の計算ロジックの修正。
- VSCode 埋め込み時のダークモードにおけるノード背景色の表示改善。
- `maxWidth` 適用時にテキストが縦長に表示される問題の修正。
- 葉ノードにおける誤った折りたたみを防ぐためのロジックの微調整。
